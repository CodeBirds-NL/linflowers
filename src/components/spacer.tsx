import React from "react"

const Spacer = ({ height = "4rem" }) => (
  <div
    style={{
      height,
      width: "100%",
      backgroundColor: "transparent",
    }}
  ></div>
)

export default Spacer
